# This starter workflow is for a CMake project running on multiple platforms. There is a different starter workflow if you just want a single platform.
# See: https://github.com/actions/starter-workflows/blob/main/ci/cmake-single-platform.yml
name: Build Protobuf on multiple platforms

on:
  push:
    branches: ['**']

jobs:
  build:
    runs-on: ${{ matrix.os }}
    timeout-minutes: 360

    strategy:
      fail-fast: false
      matrix:
        os: [windows-2022]
        build_type: [Debug]
        protobuf_shared: [ON]

    steps:
    - uses: actions/checkout@v4

    - name: Build protobuf
      id: build_step
      continue-on-error: true
      shell: bash
      env:
        BUILD_TYPE: ${{ matrix.build_type }}
        PROTOBUF_SHARED: ${{ matrix.protobuf_shared }}
      run: |
        ./build_protobuf.sh

    # - name: Debugging with tmate
    #   if: ${{ always() && matrix.os == 'windows-2022' && steps.build_step.outcome == 'failure' }}
    #   uses: mxschmitt/action-tmate@v3

    - name: Fail job if build failed
      if: ${{ steps.build_step.outcome == 'failure' }}
      shell: bash
      run: |
        echo "Build step failed; preserving failed job status after optional tmate session."
        exit 1

